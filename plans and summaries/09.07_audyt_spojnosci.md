# Audyt spójności — martwy kod, logika, powielenia (2026-07-09)

Audyt po ostatnich refaktorach (embedded-only, krzywe pierścieni, paleta raportu,
skrypty walidacji/strojenia). Cel: wychwycić wszystko, co może zaburzyć działanie
„bez naszej wiedzy”.

## Zweryfikowane — BEZ błędów runtime
- `n_peaks` czysto przemianowany na `n_rings` (brak wiszącej referencji → brak NameError).
- Brak importów cyklicznych — `ring_extraction` importowany **leniwie** w `draw_reasoning_card`.
- Ścieżka **embedded-only** spójna: tylko `emb_on_emb` przepływa; `cond_models`/`pred_csvs`/
  `results_dfs`/`candidate_overlays`/logi NotEmbedded (puste) obsłużone; raport z notką 1-warunkową.
- `_fallback_axis` (żywy w `run_candidates`), `ring_detection.*` (zaparkowany, za flagą
  `detect_image_rings` + w `tune_image_rings.py`) — używane, zostają.

## Naprawione

### 1. Martwy kod usunięty (+ testy) — 3 generacje panelu 5 i legacy karta
Panel 5 przeszedł: strefy (paski) → skalowane kontury → **krzywe** (aktualne). Dwie martwe
generacje + legacy karta nie miały żadnego wywołania produkcyjnego (tylko definicje + testy):
- `src/visualization.py`: `compute_ring_zones`, `_colorize_zones`, `_ZONE_PALETTE`,
  `_draw_concentric_rings`, legacy karta `compute_dot_positions`/`draw_increment_card`/
  `save_increment_cards`, oraz nieużywany już import `matplotlib`/`plt`.
- `src/otolith_axis.py`: `project_distance_to_axis` (używane tylko przez martwe `compute_ring_zones`).
- `src/candidates.py`: `extract_radial_profile` (legacy fallback; zastąpione
  `sample_profile_along_axis`) + nieużywany import `Union`.
- Usunięte testy: `test_stage10_visualization.py` (legacy karta, strefy, koncentryczne),
  `test_otolith_axis.py` (`project_distance_to_axis`), `test_stage7_candidates.py`
  (`extract_radial_profile`). Łącznie −13 testów.

### 2. Nieużywany import
- `src/comparison_report.py`: usunięty `from typing import Any`.

### 3. Spójność karty — panele 4/5/6 to teraz JEDEN estymat
Wcześniej: panel 5 = krzywe (wielokierunkowe), panel 6 = kropki z peaków **jednej** osi,
werdykt = wiek (CORAL) → trzy niezależne liczby, mogły się rozjeżdżać.
- `src/ring_extraction.py`: dodane `extract_rings(...) -> [(mean_t, curve)]`;
  `extract_ring_curves` to teraz cienki wrapper (testy bez zmian).
- `src/visualization.py::draw_reasoning_card`: kropki wyznaczane z **przecięć krzywych z osią**
  (`mean_t → index na osi`); ten sam zestaw kropek trafia do paneli 4, 5 i 6 → **#kropek =
  #krzywych** i zawsze się pokrywają. `peak_indices` z wejścia = tylko fallback (gdy brak krzywych).
- Opis sekcji E (`comparison_report._section_e`) zaktualizowany: panel 3 = mapa MIL (nie „L2/nietrenowany”),
  panel 5 = krzywe, panel 6 = przecięcia z osią; dodana nota: **wiek (CORAL, liczy)** może różnić się
  od **liczby zlokalizowanych pierścieni (MIL, lokalizuje)** — po dobrym treningu powinny się zgadzać.

## Wynik
`python -m pytest` → **250 passed, 0 failed** (263 − 13 usuniętych testów martwego kodu).
Karta zweryfikowana wizualnie: panele 4/5/6 spójne (2 krzywe ↔ 2 kropki ↔ 2 markery profilu).
